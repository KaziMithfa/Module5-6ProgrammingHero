const a = 56;
const numbers = [56, 7, 8];
const person = {
  name: "Sakib",
};

const message = `Hi, ${person.name} has a: ${a} access to ${numbers[0]}`;
console.log(message);

//arrow functiion

const square = (x) => x * x;
const sum = (a, b) => a + b;
